# Declare variable
age <- 10
marks1 <- 20.10
subject_name <- 'R'

# cannot use 
# $subject_name <- 'Java'
# 3name <- 'Alex'

# variable properties
typeof(age) # double
mode(age) # numeric
storage.mode(age) # double
class(age)

typeof(subject_name)
mode(subject_name)
storage.mode(subject_name)
class(subject_name)

typeof(marks1)
mode(marks1)
storage.mode(marks1)
class(marks1)

flag = TRUE
typeof(flag)
mode(flag)
storage.mode(flag)
class(flag)
