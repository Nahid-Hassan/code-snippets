import subprocess
import time
from pprint import pprint
from datetime import datetime
from datetime import timedelta
import os

# out = subprocess.run(['ls', '-la'])

# print(out.args)
# print(out.returncode)

# out = subprocess.run(['ls'], capture_output=True)
# out = out.stdout # return a byte string
# out = out.decode('utf-8') # convert bytestring to string using utf-8 encoding

# pprint(out.split('\n'))

# out = subprocess.run(['ls', '-al'], capture_output=True)

# print(out.stdout.decode())

# out = subprocess.run(['ls', '-al'], capture_output=True, text=True)

# print(out.stdout)
